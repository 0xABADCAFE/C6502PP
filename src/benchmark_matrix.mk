BENCHMARK_TARGETS := Runtime RuntimeLTO StaticSC StaticSCPin StaticMaxGoto

BENCH_FLAGS_Runtime := -DINTERPRET_SWITCH -DUNPINNED -fno-lto
BENCH_FLAGS_RuntimeLTO := -DINTERPRET_SWITCH -DUNPINNED -flto
BENCH_FLAGS_StaticSC := -DSTATIC_SYSTEM -DINTERPRET_SWITCH -DUNPINNED -fno-lto
BENCH_FLAGS_StaticSCPin := -DSTATIC_SYSTEM -DINTERPRET_SWITCH -fno-lto
BENCH_FLAGS_StaticMaxGoto := -DSTATIC_SYSTEM -fno-lto

BENCH_LDFLAGS_Runtime :=
BENCH_LDFLAGS_RuntimeLTO := -flto
BENCH_LDFLAGS_StaticSC :=
BENCH_LDFLAGS_StaticSCPin :=
BENCH_LDFLAGS_StaticMaxGoto :=

