BENCHMARK_TARGETS := Runtime RuntimeLTO StaticSC StaticSCLTO StaticSCPin StaticSCPinLTO StaticMaxGoto StaticMaxGotoLTO

BENCH_FLAGS_Runtime := -DINTERPRET_SWITCH -DUNPINNED -fno-lto
BENCH_FLAGS_RuntimeLTO := -DINTERPRET_SWITCH -DUNPINNED -flto
BENCH_FLAGS_StaticSC := -DSTATIC_SYSTEM -DINTERPRET_SWITCH -DUNPINNED -fno-lto
BENCH_FLAGS_StaticSCLTO := -DSTATIC_SYSTEM -DINTERPRET_SWITCH -DUNPINNED -flto
BENCH_FLAGS_StaticSCPin := -DSTATIC_SYSTEM -DINTERPRET_SWITCH -fno-lto
BENCH_FLAGS_StaticSCPinLTO := -DSTATIC_SYSTEM -DINTERPRET_SWITCH -flto
BENCH_FLAGS_StaticMaxGoto := -DSTATIC_SYSTEM -fno-lto
BENCH_FLAGS_StaticMaxGotoLTO := -DSTATIC_SYSTEM -flto

BENCH_LDFLAGS_Runtime := -fno-lto
BENCH_LDFLAGS_RuntimeLTO := -flto
BENCH_LDFLAGS_StaticSC := -fno-lto
BENCH_LDFLAGS_StaticSCLTO := -flto
BENCH_LDFLAGS_StaticSCPin := -fno-lto
BENCH_LDFLAGS_StaticSCPinLTO := -flto
BENCH_LDFLAGS_StaticMaxGoto := -fno-lto
BENCH_LDFLAGS_StaticMaxGotoLTO := -flto

